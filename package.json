{
  "name": "statespace-mcp",
  "version": "0.1.0",
  "description": "MCP server for Statespace apps",
  "type": "module",
  "bin": {
    "statespace-mcp": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && rm -rf dist/prompts && cp -r src/prompts dist/prompts",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.2"
  }
}
